# 千岛 · C2C 业务组件规范

> PM 涉及 C2C 模块的需求时，AI 读取本文件。
> 全局规则见 `00 CLAUDE.md`，页面结构见同目录 `*.html`。

---

## 状态

⚠️ 本文件尚未填充。设计师请参照 `community/_module.md` 的格式补充：
1. 页面模板（从上到下的组件堆叠顺序）
2. 业务组件 Key 表（运行 `05 figma-plugin/extract-business-keys.js` 提取）
3. 变体选择指引
